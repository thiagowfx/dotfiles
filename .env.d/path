addpath() {
    addenv PATH "$1:$PATH" "$2" "$3"
}

addpath "$HOME/.bin" "" "$HOME/.bin"
command -v ruby &>/dev/null && addpath "$(ruby -rubygems -e "puts Gem.user_dir")/bin" ruby "$(ruby -rubygems -e "puts Gem.user_dir")/bin"
